# Dotfiles

1. Move the configuration files to the appropriate locations manually.  
2. Install the required packages by running:  

```bash
    sudo -E ./install_packages.sh
 ```
3. If you are using a proxy, make sure to set it before rerunning the script.
